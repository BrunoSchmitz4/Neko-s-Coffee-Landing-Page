// // Este arquivo será usado para para o locahost com o Node JS / Nodemon (sem depender do Live Server).

// var http = require('http');
// // O código acima faz a requisição do módulo HTTP (nativo do NodeJs) e inicializa numa variável.

// // Const é umavariável não alterável, e o m[odulo express (framework) ajuda no desenvolvimento web com o necessário, e claro, auxilia no back-end (futuramente será mostrado).
// const express = require('express');
// const app = express();




// // Aqui, a variável com este módulo cria um servidor, usando uma função (req, res) que, vai mostrar a msg abaixo e abrir na porta desejada, ouseja, 8080.
// http.createServer(function(req, res){
//     res.end("Server aberto com sucesso");
// }).listen();

// //Rotas
// app.get('/', function(req, res){
//     res.render('pagamento');
// });

// app.listen(3010);


// Código obsoleto, necvessário verificar com o Celke